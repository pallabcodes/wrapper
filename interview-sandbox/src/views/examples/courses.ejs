<% 
  // ============================================================================
  // GENERIC CRUD PAGE TEMPLATE - Works with ANY entity type
  // ============================================================================
  // This is just an EXAMPLE using "Courses" - you can use this for:
  // - Todos, Tasks, Notes, Products, Orders, Users, Categories, etc.
  // 
  // To adapt for your entity, simply modify the crudConfig object below:
  // - title: Display name (e.g., 'Todos', 'Products', 'Tasks')
  // - entityName: Lowercase singular (e.g., 'todo', 'product', 'task')
  // - apiEndpoint: Your API endpoint (e.g., '/api/todos', '/api/products')
  // - headers: Table column headers
  // - columns: Property names from your API response
  // - fields: Form fields for create/edit modal
  // ============================================================================
  
  const crudConfig = {
    title: 'Courses',  // Change to: 'Todos', 'Products', 'Tasks', etc.
    entityName: 'course',  // Change to: 'todo', 'product', 'task', etc.
    apiEndpoint: '/api/courses',  // Change to: '/api/todos', '/api/products', etc.
    headers: ['Code', 'Name', 'Instructor', 'Students', 'Status'],
    columns: ['code', 'name', 'instructorName', 'studentCount', 'status'],
    fields: [
      { name: 'code', label: 'Course Code', type: 'text', required: true },
      { name: 'name', label: 'Course Name', type: 'text', required: true },
      { name: 'instructorId', label: 'Instructor', type: 'select', required: true, options: [
        { value: '1', label: 'Dr. Smith' },
        { value: '2', label: 'Prof. Johnson' }
      ]},
      { name: 'description', label: 'Description', type: 'textarea', required: false },
      { name: 'status', label: 'Status', type: 'select', required: true, options: [
        { value: 'active', label: 'Active' },
        { value: 'inactive', label: 'Inactive' }
      ]}
    ],
    searchable: true,
    createButton: true
  };
%>

<%- include('../components/crud-list', crudConfig) %>

<%- include('../layouts/base', { 
  title: 'Courses - Platform',
  body: '',
  scripts: ''
}) %>

