/**
 * Global Test Teardown - Enterprise Grade Testing
 * 
 * Cleans up test environment and resources after all tests.
 * Following internal team testing standards.
 */

import { globalTeardown as cleanup } from './globalSetup'

export default cleanup
