import { Router } from 'express';

const router = Router();

// Placeholder routes - to be implemented
router.get('/stock', (req, res) => {
  res.json({ message: 'Inventory stock endpoint' });
});

export default router;
