import { Router } from 'express';

const router = Router();

// Placeholder routes - to be implemented
router.get('/rooms', (req, res) => {
  res.json({ message: 'Chat rooms endpoint' });
});

export default router;
